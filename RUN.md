# 🚀 دليل تشغيل متجر DeltaStars

## نظرة عامة

متجر DeltaStars هو تطبيق ويب متكامل لبيع الفواكه والخضروات الطازجة والتمور الفاخرة في المملكة العربية السعودية.

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- **Node.js** (الإصدار 18.x أو أحدث)
- **npm** (يأتي مع Node.js)

للتحقق من التثبيت:
```bash
node --version
npm --version
```

## ⚡ التشغيل السريع

### 1. تثبيت المكتبات المطلوبة

```bash
npm install
```

### 2. إعداد متغيرات البيئة

قم بتحديث ملف `.env.local` بمفتاح Gemini API الخاص بك:

```bash
GEMINI_API_KEY=your_actual_api_key_here
```

### 3. تشغيل التطبيق

```bash
npm run dev
```

سيعمل التطبيق على `http://localhost:3000` (أو المنفذ التالي المتاح إذا كان 3000 مشغولاً).

## 🌐 الوصول للتطبيق

بعد التشغيل، يمكنك الوصول إلى:

- **الصفحة الرئيسية**: `http://localhost:3000`
- **صفحة المنتجات**: `http://localhost:3000/#products`
- **صفحة تسجيل الدخول**: `http://localhost:3000/#login`
- **لوحة التحكم**: `http://localhost:3000/#dashboard`

## 📦 البناء للإنتاج

### بناء التطبيق

```bash
npm run build
```

سيتم إنشاء ملفات الإنتاج في مجلد `dist/`.

### معاينة الإنتاج محلياً

```bash
npm run preview
```

## 🗂️ بنية المشروع

```
webapp/
├── index.html              # نقطة الدخول HTML
├── index.tsx              # نقطة الدخول React
├── App.tsx                # المكون الرئيسي
├── package.json           # التبعيات والإعدادات
├── vite.config.ts         # إعدادات Vite
├── tsconfig.json          # إعدادات TypeScript
├── .env.local            # متغيرات البيئة
│
├── lib/                   # مكتبات ومكونات مشتركة
│   ├── Header.tsx        # رأس الصفحة
│   ├── Home.tsx          # الصفحة الرئيسية
│   ├── Icons.tsx         # أيقونات SVG
│   ├── ImageZoomModal.tsx # عارض الصور
│   └── api.ts            # عميل API
│
├── contexts/              # سياقات React
│   ├── I18nContext.tsx   # دعم اللغات
│   ├── ToastContext.tsx  # الإشعارات
│   └── SettingsContext.tsx # الإعدادات
│
├── styles/               # ملفات CSS
│   └── index.css        # الأنماط العامة
│
└── [مكونات أخرى].tsx    # مكونات الصفحات المختلفة
```

## 🛠️ الأوامر المتاحة

| الأمر | الوصف |
|------|------|
| `npm install` | تثبيت المكتبات المطلوبة |
| `npm run dev` | تشغيل الخادم التطويري |
| `npm run build` | بناء التطبيق للإنتاج |
| `npm run preview` | معاينة الإنتاج محلياً |

## 🔧 حل المشاكل الشائعة

### المشكلة: خطأ في تثبيت المكتبات

**الحل:**
```bash
# امسح مجلد node_modules و package-lock.json
rm -rf node_modules package-lock.json

# أعد التثبيت
npm install
```

### المشكلة: المنفذ 3000 مشغول

**الحل:**
سيختار Vite تلقائياً المنفذ التالي المتاح (3001، 3002، إلخ).

أو يمكنك تحديد منفذ مختلف في `vite.config.ts`:
```typescript
server: {
  port: 3002, // أي منفذ ترغب به
  host: '0.0.0.0'
}
```

### المشكلة: خطأ في استيراد الملفات

**الحل:**
تأكد من أن جميع الملفات موجودة في المسارات الصحيحة كما هو موضح في بنية المشروع أعلاه.

### المشكلة: TypeScript Errors

**الحل:**
```bash
# التحقق من الأخطاء
npx tsc --noEmit

# إعادة تشغيل الخادم
npm run dev
```

## 🌟 المميزات الرئيسية

### 1. واجهة متعددة اللغات
- العربية (افتراضي)
- الإنجليزية

### 2. نظام السلة والطلبات
- إضافة/إزالة المنتجات
- حساب الإجمالي تلقائياً
- حفظ السلة في Local Storage

### 3. نظام المستخدمين
- تسجيل الدخول/التسجيل
- صفحات شخصية
- لوحة تحكم للمستخدمين

### 4. نظام VIP للعملاء الكبار
- صفحة دخول خاصة
- لوحة تحكم متقدمة
- إدارة الطلبات الكبيرة

### 5. الذكاء الاصطناعي
- مساعد AI مدعوم بـ Gemini
- توصيات ذكية
- دعم العملاء التلقائي

## 📱 التجاوب مع الأجهزة

التطبيق متجاوب بالكامل ويعمل على:
- 💻 أجهزة الكمبيوتر
- 📱 الهواتف الذكية
- 📲 الأجهزة اللوحية

## 🔐 الأمان

- كلمات مرور مشفرة
- JWT للمصادقة
- HTTPS في الإنتاج
- حماية من XSS و CSRF

## 📞 معلومات الاتصال

**شركة نجوم دلتا للتجارة**

- 📧 البريد الإلكتروني: INFO@DELTASTARS-KSA.COM
- ☎️ الهاتف الأرضي: 00966920023204
- 📱 واتساب: 00966558828009
- 🌐 الموقع: https://deltastars-ksa.com
- 📍 العنوان: جدة، شارع المنار، المملكة العربية السعودية

## 🤝 الدعم الفني

إذا واجهت أي مشاكل:

1. تحقق من سجل الأخطاء في Console
2. راجع قسم حل المشاكل أعلاه
3. تواصل معنا عبر واتساب: 00966558828009

## 📝 ملاحظات مهمة

### بيانات تجريبية

التطبيق يحتوي حالياً على بيانات تجريبية (mock data):
- المنتجات: `mockProducts` في `products.ts`
- الفواتير: `mockInvoices` في `accounting.ts`
- العملاء VIP: `mockVipClients` في `accounting.ts`

### الاتصال بـ API حقيقي

لربط التطبيق بـ API حقيقي:

1. قم بتحديث `API_BASE_URL` في `lib/api.ts`
2. قم بإعداد متغيرات البيئة في `.env.local`:
```bash
VITE_API_BASE_URL=https://api.yourserver.com
GEMINI_API_KEY=your_gemini_api_key
```

### النشر على الإنتاج

#### Vercel
```bash
npm install -g vercel
vercel
```

#### Netlify
```bash
npm install -g netlify-cli
netlify deploy --prod
```

#### Docker
```bash
# بناء صورة Docker
docker build -t deltastars-store .

# تشغيل الحاوية
docker run -p 3000:3000 deltastars-store
```

## 🎨 التخصيص

### تغيير الألوان
عدّل المتغيرات في `styles/index.css`:
```css
:root {
  --color-primary: #667eea;
  --color-secondary: #764ba2;
  /* ... */
}
```

### تغيير الخطوط
عدّل روابط الخطوط في `index.html`:
```html
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700&display=swap" rel="stylesheet">
```

## 📊 الأداء

### نصائح لتحسين الأداء:

1. **تحسين الصور**: استخدم صيغ WebP أو AVIF
2. **Lazy Loading**: للصور والمكونات
3. **Code Splitting**: يتم تلقائياً بواسطة Vite
4. **Caching**: استخدم Service Workers
5. **CDN**: لاستضافة الملفات الثابتة

## 🔄 التحديثات المستقبلية

- [ ] تكامل مع بوابات الدفع
- [ ] تطبيق موبايل (React Native)
- [ ] نظام التقييمات والمراجعات
- [ ] برنامج الولاء والنقاط
- [ ] إشعارات Push
- [ ] تتبع الطلبات في الوقت الفعلي

## 📜 الترخيص

© 2024 DeltaStars. جميع الحقوق محفوظة.

---

**آخر تحديث:** فبراير 2026
**الإصدار:** 1.0.0

🌟 **شكراً لاستخدامك متجر DeltaStars!** 🌟
